# Install the CPU-only PyTorch build (no CUDA/NVIDIA libraries) on Linux.
# PyPI's default linux torch wheel bundles CUDA; the +cpu wheel from the
# PyTorch CPU index is far smaller and is all this app needs (inference is CPU).
--extra-index-url https://download.pytorch.org/whl/cpu

# --- Core RAG pipeline ---
faiss-cpu>=1.8.0
rank-bm25>=0.2.2
sentence-transformers>=2.7.0
transformers>=4.40.0
torch==2.7.1+cpu
numpy>=1.26.0
scikit-learn>=1.4.0

# --- PDF ingestion ---
pdfplumber>=0.11.0

# --- Multimodal ingestion (OPT-IN via ENABLE_VISUAL_INGEST) ---
# PyMuPDF renders page images and detects embedded figures/images; pytesseract
# drives OCR for scanned pages / image-based tables / text inside figures.
# OCR additionally requires the Tesseract engine binary to be installed.
PyMuPDF>=1.24.0
pytesseract>=0.3.10

# --- Data / evaluation ---
pandas>=2.2.0

# --- API ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
requests>=2.31.0
python-dotenv>=1.0.0

# --- Testing ---
pytest>=8.0.0
httpx>=0.27.0
